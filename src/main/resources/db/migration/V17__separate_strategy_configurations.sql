-- Единственная строка id='current' раньше перезаписывалась настройками другой стратегии.
-- Сохраняем существующую конфигурацию под идентификатором её типа.
UPDATE strategy_config
SET id = lower(type::text)
WHERE id = 'current';
