meta:
  environment: jvm
  toolchain:
    name: gradle
    version: "17"
build:
  image: null
  args: "./gradlew clean build -x test"  # ← Добавлено: сборка без тестов
  artifacts:
    build/libs/*.jar: /
run:
  persistenceMount: /data
  image: null
  jarName: main.jar
  containerPort: 80
  servicePort: 80
